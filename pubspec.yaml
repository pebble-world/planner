name: planner
description: A calendar event schedule interface.
version: 0.0.1
author: Mute <info@mutecode.com>
homepage: http://forum.mutecode.com/

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  after_layout: ^1.0.7
  vibration: ^1.0.2
  positioned_tap_detector: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

