name: planner
description: A calendar event schedule interface. Displays events in an editable grid with day and hour labels.
version: 0.1.0
author: Mute <info@mutecode.com>
homepage: https://github.com/ourtrip/planner
repository: https://github.com/ourtrip/planner

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  after_layout: ^1.0.7+2
  vibration: ^1.2.4
  positioned_tap_detector: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

